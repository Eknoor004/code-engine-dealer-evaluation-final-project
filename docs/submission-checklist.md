# Submission Checklist

Capture and save the required evidence files for final grading:

1. `product_details_deploy.png` or `.jpeg` - successful Product Details deployment
2. `dealer_details_details.png` or `.jpeg` - successful Dealer Pricing deployment
3. `git_clone.png` or `.jpeg` - successful frontend repository clone
4. `index_urlchanges.png` or `.jpeg` - updated API URLs in `index.html`
5. `frontend_deploy.png` or `.jpeg` - successful frontend deployment
6. `homepage.png` or `.jpeg` - homepage with product list visible
7. `product_dealer.png` or `.jpeg` - selected product with dealer list
8. `product_dealer_price.png` or `.jpeg` - selected dealer with returned price
9. `product_all_dealers_prices.png` or `.jpeg` - all dealers pricing table

Before submission:

- Confirm each backend and frontend URL opens successfully.
- Confirm URL values in `frontend/html/index.html` end with `/`.
- Confirm screenshots are from your own deployment session.
